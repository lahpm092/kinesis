# --- Python env ---
.venv/
__pycache__/
*.pyc

# --- Gated / large model weights (do NOT redistribute; see README) ---
models/

# --- Heavy pipeline inputs & intermediates (raw footage, match videos, QA shots) ---
/data/

# --- Node ---
node_modules/
web/dist/
# scratch build dirs used by the QA sweeps (dist2, dist3, …)
dist[0-9]*/
dist/

# --- OS / editor ---
.DS_Store
*.log
scratchpad/
